## Child Page TOC

The right-side page table of contents includes the rendered page heading on
grouped child pages, even when the child page has only one major section.

### Tracked Subsection

When a page has multiple visible headings, the right-side table of contents
tracks the current section as the reader scrolls.
