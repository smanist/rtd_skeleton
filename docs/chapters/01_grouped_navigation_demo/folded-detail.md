## Folded Detail Page

This child page intentionally starts with a level-2 heading. Grouped chapter
children preserve moved section heading levels, and the site suppresses the
expected MyST warning for that shape.

:::{foldbox} Optional calculation

The `foldbox` directive is useful for details that support the main line of the
notes without interrupting it:

```{math}
\ddf{x}{t} = a x,
\qquad
x(t) = x(0)\exp(a t).
```

:::
