display_name: Split Chapter Pages
short_description: Split a MyST chapter into a grouped chapter folder.
default_prompt: Split the selected chapter into a grouped set of pages, preserving the original page as the foreword and Summary with links to child pages.
